import React, { Component } from "react"
import Button from "react-bootstrap/Button"

class TestComponent extends Component {
  render() {
    return (
      <div>
        Test component using bootstrap <Button>Button</Button>
      </div>
    )
  }
}

export default TestComponent
